"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var env_1 = __importDefault(require("../__mocks__/env"));
describe('configs', function () {
    test('load from env', function () {
        var restoreEnv = (0, env_1["default"])({
            SSC_API_TOKEN: 'some_token',
            SSC_API_HOST: 'http://platform-api.securityscorecard.io'
        });
        var configs;
        jest.isolateModules(function () {
            // eslint-disable-next-line global-require
            var configsModule = require('../configs');
            configs = configsModule["default"];
        });
        expect(configs).toEqual({ token: 'some_token', host: 'http://platform-api.securityscorecard.io' });
        restoreEnv();
    });
});
//# sourceMappingURL=configs.spec.js.map