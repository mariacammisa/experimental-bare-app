import { SecurityScorecardApi, ApiCallOptions } from './types';
export default class SecurityScorecardAPI implements SecurityScorecardApi {
    /**
     * Default timeout in milliseconds 6000ms equivalent to 1 minute
     */
    static DEFAULT_TIMEOUT_MS: number;
    /**
     * Default number of retries, by default this value is set to 0.
     * Retries can only be used with requests that are idempotent.
     */
    static DEFAULT_MAX_RETRIES: number;
    /**
     * By default the sdk will use production 'https://platform-api.securityscorecard.io'
     */
    static DEFAULT_HOST: string;
    config: {
        token: string;
        timeout: number;
        host: string;
        maxRetries: number;
    };
    constructor(token: string, options?: {
        timeout?: number;
        maxRetries?: number;
        host?: string;
    });
    apiCall<ResponseType = any>(path: string, options?: ApiCallOptions): Promise<ResponseType | undefined>;
}
//# sourceMappingURL=api.d.ts.map